diff -uNr a/src/nvim/drawscreen.c b/src/nvim/drawscreen.c
--- a/src/nvim/drawscreen.c	2026-09-21 23:24:15.337891905 +0300
+++ b/src/nvim/drawscreen.c	2026-09-21 23:24:22.127924317 +0300
@@ -437,12 +437,11 @@
 /// and redraw_all_later() to mark parts of the screen as needing a redraw.
 int update_screen(void)
 {
-  static bool still_may_intro = true;
-  if (still_may_intro) {
-    if (!may_show_intro()) {
-      redraw_later(firstwin, UPD_NOT_VALID);
-      still_may_intro = false;
-    }
+  static bool was_intro = false;
+  bool show_intro = may_show_intro();
+  if (was_intro != show_intro) {
+    redraw_all_later(UPD_NOT_VALID);
+    was_intro = show_intro;
   }
 
   bool is_stl_global = global_stl_height() > 0;
@@ -717,7 +716,7 @@
   }
 
   // May put up an introductory message when not editing a file
-  if (still_may_intro) {
+  if (may_show_intro()) {
     intro_message(false);
   }
   repeat_message();
diff -uNr a/src/nvim/eval/funcs.c b/src/nvim/eval/funcs.c
--- a/src/nvim/eval/funcs.c	2026-09-21 23:24:15.338157085 +0300
+++ b/src/nvim/eval/funcs.c	2026-09-21 23:24:22.128195087 +0300
@@ -3625,6 +3625,13 @@
   }
 
   dict_T *env = create_environment(job_env, clear_env, pty, true, term_name);
+  if (term) {
+    dictitem_T *dv = tv_dict_find(env, S_LEN("NVIM_BUF_ID"));
+    if (dv) {
+      tv_dict_item_remove(env, dv);
+    }
+    tv_dict_add_nr(env, S_LEN("NVIM_BUF_ID"), curbuf->b_fnum);
+  }
   Channel *chan = channel_job_start(argv, NULL, on_stdout, on_stderr, on_exit, pty,
                                     rpc, overlapped, detach, stdin_mode, cwd,
                                     width, height, env, &rettv->vval.v_number);
diff -uNr a/src/nvim/getchar.c b/src/nvim/getchar.c
--- a/src/nvim/getchar.c	2026-09-21 23:24:15.339608830 +0300
+++ b/src/nvim/getchar.c	2026-09-21 23:24:42.560676665 +0300
@@ -2232,7 +2232,13 @@
     // First try buffer-local mappings.
     mp = get_buf_maphash_list(local_State, tb_c1);
     mp2 = get_maphash_list(local_State, tb_c1);
+    bool is_pager = (curbuf != NULL && curbuf->b_vars != NULL && tv_dict_find(curbuf->b_vars, S_LEN("is_pager")) != NULL);
+    if (curbuf != NULL && curbuf->terminal != NULL && !is_pager && (local_State & (MODE_NORMAL | MODE_VISUAL))) {
+      if (tb_c1 != K_SPECIAL && (uint8_t)tb_c1 != Ctrl_C) {
+        mp2 = NULL;
+      }
+    }
     if (mp == NULL) {
       // There are no buffer-local mappings.
       mp = mp2;
       mp2 = NULL;
@@ -2646,7 +2651,8 @@
           line_breakcheck();
         } else {
           // os_breakcheck() can call input_enqueue()
-          if ((mapped_ctrl_c | curbuf->b_mapped_ctrl_c) & get_real_state()) {
+          if ((curbuf != NULL && curbuf->terminal != NULL)
+              || ((mapped_ctrl_c | curbuf->b_mapped_ctrl_c) & get_real_state())) {
             ctrl_c_interrupts = false;
           }
           os_breakcheck();  // check for CTRL-C
diff -uNr a/src/nvim/normal.c b/src/nvim/normal.c
--- a/src/nvim/normal.c	2026-09-21 23:24:15.341138423 +0300
+++ b/src/nvim/normal.c	2026-09-21 23:24:22.128516613 +0300
@@ -31,6 +31,7 @@
 #include "nvim/errors.h"
 #include "nvim/eval.h"
 #include "nvim/eval/vars.h"
+#include "nvim/eval/typval.h"
 #include "nvim/ex_cmds.h"
 #include "nvim/ex_docmd.h"
 #include "nvim/extmark.h"
@@ -1080,6 +1081,25 @@
 
   LANGMAP_ADJUST(s->c, get_real_state() != MODE_SELECT);
 
+  bool is_pager = (curbuf != NULL && curbuf->b_vars != NULL && tv_dict_find(curbuf->b_vars, S_LEN("is_pager")) != NULL);
+  if (curbuf->terminal != NULL && !is_pager) {
+    bool is_whitelisted = is_mouse_key(s->c)
+                          || s->c == K_EVENT || s->c == K_IGNORE
+                          || !KeyTyped
+                          || s->mapped_len > 0
+                          || typebuf_maplen() > 0;
+    if (!is_whitelisted) {
+      if (VIsual_active) {
+        end_visual_mode();
+      }
+      got_int = false;
+      terminal_send_key(curbuf->terminal, s->c);
+      terminal_enter();
+      s->command_finished = true;
+      return 1;
+    }
+  }
+
   // If a mapping was started in Visual or Select mode, remember the length
   // of the mapping.  This is used below to not return to Insert mode for as
   // long as the mapping is being executed.
diff -uNr a/src/nvim/os/input.c b/src/nvim/os/input.c
--- a/src/nvim/os/input.c	2026-09-21 23:24:15.341794700 +0300
+++ b/src/nvim/os/input.c	2026-09-21 23:24:38.480624681 +0300
@@ -128,8 +128,9 @@
 
   TRY_READ();
 
-  // No risk of a UI flood, so disable CTRL-C "interrupt" behavior if it's mapped.
-  if ((mapped_ctrl_c | curbuf->b_mapped_ctrl_c) & get_real_state()) {
+  // No risk of a UI flood, so disable CTRL-C "interrupt" behavior if it's mapped or in a terminal.
+  if ((curbuf != NULL && curbuf->terminal != NULL)
+      || ((mapped_ctrl_c | curbuf->b_mapped_ctrl_c) & get_real_state())) {
     ctrl_c_interrupts = false;
   }
 
@@ -538,7 +539,7 @@
 
 static void process_ctrl_c(void)
 {
-  if (!ctrl_c_interrupts) {
+  if (!ctrl_c_interrupts || (curbuf != NULL && curbuf->terminal != NULL)) {
     return;
   }
 
diff -uNr a/src/nvim/register.c b/src/nvim/register.c
--- a/src/nvim/register.c	2026-09-21 23:24:15.343984827 +0300
+++ b/src/nvim/register.c	2026-09-21 23:24:22.128716450 +0300
@@ -1051,30 +1051,55 @@
     }
   }
 
-  for (; lnum <= yankendlnum; lnum++, y_idx++) {
-    switch (reg->y_type) {
-    case kMTBlockWise:
-      block_prep(oap, &bd, lnum, false);
-      yank_copy_line(reg, &bd, y_idx, oap->excl_tr_ws);
-      break;
-
-    case kMTLineWise:
-      reg->y_array[y_idx] = cbuf_to_string(ml_get(lnum), (size_t)ml_get_len(lnum));
-      break;
-
-    case kMTCharWise:
-      charwise_block_prep(oap->start, oap->end, &bd, lnum, oap->inclusive);
-      // make sure bd.textlen is not longer than the text
-      int tmp = (int)strlen(bd.textstart);
-      if (tmp < bd.textlen) {
-        bd.textlen = tmp;
+  for (; lnum <= yankendlnum; lnum++) {
+    bool is_cont = (curbuf->terminal != NULL && lnum > oap->start.lnum && terminal_line_is_continuation(curbuf, lnum));
+    size_t cur_y_idx = (is_cont && y_idx > 0) ? y_idx - 1 : y_idx;
+
+    if (is_cont && y_idx > 0) {
+      // Append text directly to previous register line without inserting \n
+      char *line_text = ml_get(lnum);
+      size_t line_len = (size_t)ml_get_len(lnum);
+      if (reg->y_type == kMTCharWise && lnum == yankendlnum) {
+        charwise_block_prep(oap->start, oap->end, &bd, lnum, oap->inclusive);
+        int tmp = (int)strlen(bd.textstart);
+        if (tmp < bd.textlen) {
+          bd.textlen = tmp;
+        }
+        line_len = (size_t)bd.textlen;
       }
-      yank_copy_line(reg, &bd, y_idx, false);
-      break;
+      String *prev = &reg->y_array[cur_y_idx];
+      size_t new_size = prev->size + line_len;
+      char *new_data = xmalloc(new_size + 1);
+      memcpy(new_data, prev->data, prev->size);
+      memcpy(new_data + prev->size, line_text, line_len);
+      new_data[new_size] = NUL;
+      xfree(prev->data);
+      *prev = cbuf_as_string(new_data, new_size);
+    } else {
+      switch (reg->y_type) {
+      case kMTBlockWise:
+        block_prep(oap, &bd, lnum, false);
+        yank_copy_line(reg, &bd, cur_y_idx, oap->excl_tr_ws);
+        break;
+
+      case kMTLineWise:
+        reg->y_array[cur_y_idx] = cbuf_to_string(ml_get(lnum), (size_t)ml_get_len(lnum));
+        break;
 
-    // NOTREACHED
-    case kMTUnknown:
-      abort();
+      case kMTCharWise:
+        charwise_block_prep(oap->start, oap->end, &bd, lnum, oap->inclusive);
+        int tmp = (int)strlen(bd.textstart);
+        if (tmp < bd.textlen) {
+          bd.textlen = tmp;
+        }
+        yank_copy_line(reg, &bd, cur_y_idx, false);
+        break;
+
+      case kMTUnknown:
+        abort();
+      }
+      y_idx++;
     }
   }
+  reg->y_size = y_idx;
 
diff -uNr a/src/nvim/terminal.c b/src/nvim/terminal.c
--- a/src/nvim/terminal.c	2026-09-21 23:24:15.344518883 +0300
+++ b/src/nvim/terminal.c	2026-09-21 23:24:22.128828852 +0300
@@ -139,6 +139,7 @@
 
 typedef struct {
   size_t cols;
+  bool continuation;
   VTermScreenCell cells[];
 } ScrollbackLine;
 
@@ -963,7 +964,9 @@
   curbuf->b_last_changedtick = buf_get_changedtick(curbuf);
 
   if (curbuf->terminal == s->term && !s->close) {
-    terminal_check_cursor();
+    if (curwin->w_topline + curwin->w_view_height >= curbuf->b_ml.ml_line_count) {
+      terminal_check_cursor();
+    }
   }
   if (restart_edit) {
     showmode();
@@ -1343,7 +1346,7 @@
   }
 }
 
-static void terminal_send_key(Terminal *term, int c)
+void terminal_send_key(Terminal *term, int c)
 {
   VTermModifier mod = VTERM_MOD_NONE;
 
@@ -1595,6 +1598,7 @@
   buf->b_locked--;
   api_clear_error(&err);
   status_redraw_buf(buf);
+  redraw_titles();
 }
 
 static int term_settermprop(VTermProp prop, VTermValue *val, void *data)
@@ -1604,6 +1608,15 @@
   switch (prop) {
   case VTERM_PROP_ALTSCREEN:
     term->in_altscreen = val->boolean;
+    buf_T *alt_buf = handle_get_buffer(term->buf_handle);
+    if (alt_buf) {
+      Error err = ERROR_INIT;
+      dict_set_var(alt_buf->b_vars,
+                   STATIC_CSTR_AS_STRING("terminal_altscreen"),
+                   BOOLEAN_OBJ(val->boolean),
+                   false, false, NULL, &err);
+      api_clear_error(&err);
+    }
     break;
 
   case VTERM_PROP_CURSORVISIBLE:
@@ -1741,6 +1754,10 @@
   }
 
   memcpy(sbrow->cells, cells, sizeof(cells[0]) * c);
+  VTermState *state = vterm_obtain_state(term->vt);
+  const VTermLineInfo *info = vterm_state_get_lineinfo(state, 0);
+  sbrow->continuation = info ? info->continuation : false;
+
   if (!term->synchronized_output) {
     set_put(ptr_t, &invalidated_terminals, term);
   }
@@ -2267,6 +2284,14 @@
   }
 
   if (c == K_MOUSEUP || c == K_MOUSEDOWN || c == K_MOUSELEFT || c == K_MOUSERIGHT) {
+    if (term->in_altscreen && !term->forward_mouse) {
+      int key = (c == K_MOUSEUP) ? K_DOWN : (c == K_MOUSEDOWN) ? K_UP : (c == K_MOUSELEFT) ? K_LEFT : K_RIGHT;
+      for (int i = 0; i < 3; i++) {
+        terminal_send_key(term, key);
+      }
+      return false;
+    }
+
     win_T *save_curwin = curwin;
     // switch window/buffer to perform the scroll
     curwin = mouse_win;
@@ -2665,7 +2690,7 @@
         continue;
       }
 
-      bool following = ml_end == wp->w_cursor.lnum + added;  // cursor at end?
+      bool following = (wp->w_cursor.lnum == ml_end - added) && (wp->w_topline + wp->w_view_height >= ml_end - added);
       if (following) {
         // "Follow" the terminal output
         wp->w_cursor.lnum = ml_end;
@@ -2723,4 +2748,26 @@
 
 // }}}
 
+bool terminal_line_is_continuation(buf_T *buf, int linenr)
+{
+  if (!buf || !buf->terminal) {
+    return false;
+  }
+  Terminal *term = buf->terminal;
+  int row = linenr_to_row(term, linenr);
+  if (row < 0) {
+    int idx = -row - 1;
+    if (idx >= 0 && (size_t)idx < term->sb_current) {
+      return term->sb_buffer[idx]->continuation;
+    }
+  } else {
+    VTermState *state = vterm_obtain_state(term->vt);
+    const VTermLineInfo *info = vterm_state_get_lineinfo(state, row);
+    if (info) {
+      return info->continuation;
+    }
+  }
+  return false;
+}
+
 // vim: foldmethod=marker
diff -uNr a/src/nvim/terminal.h b/src/nvim/terminal.h
--- a/src/nvim/terminal.h	2026-09-21 23:24:15.344533681 +0300
+++ b/src/nvim/terminal.h	2026-09-21 23:24:22.128906498 +0300
@@ -24,4 +24,7 @@
   bool force_crlf;
 } TerminalOptions;
 
+bool terminal_line_is_continuation(buf_T *buf, int linenr);
+void terminal_send_key(Terminal *term, int c);
+
 #include "terminal.h.generated.h"
diff -uNr a/src/nvim/version.c b/src/nvim/version.c
--- a/src/nvim/version.c	2026-09-21 23:24:15.345255191 +0300
+++ b/src/nvim/version.c	2026-09-21 23:24:22.128981850 +0300
@@ -4219,8 +4219,7 @@
 {
   return (buf_is_empty(curbuf)
           && (curbuf->b_fname == NULL)
-          && (curbuf->handle == 1)
-          && (curwin->handle == LOWEST_WIN_ID)
+          && curbuf->terminal == NULL
           && one_window(curwin, NULL)
           && (vim_strchr(p_shm, SHM_INTRO) == NULL));
 }
